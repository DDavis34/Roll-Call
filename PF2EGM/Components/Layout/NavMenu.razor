<nav class="bg-gray-800 shadow-lg w-full">
    <div class="w-full px-4 py-4 flex flex-wrap justify-between items-center">
        
        <!-- Logo + Hamburger row -->
        <div class="flex justify-between items-center w-full lg:w-auto">
            <h1 class="text-2xl font-bold text-amber-400">Roll Call</h1>

            <!-- Hamburger button (hidden on lg+) -->
            <button 
                onclick="this.closest('nav').querySelector('#nav-menu').classList.toggle('hidden')"
                class="lg:hidden text-gray-300 hover:text-amber-400 focus:outline-none"
                aria-label="Toggle navigation">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                        d="M4 6h16M4 12h16M4 18h16" />
                </svg>
            </button>
        </div>

        <!-- Nav links -->
        <div id="nav-menu" class="hidden w-full lg:flex lg:w-auto lg:space-x-6 flex-col lg:flex-row mt-3 lg:mt-0 space-y-2 lg:space-y-0">
            <a href="#" class="text-gray-300 hover:text-amber-400 block lg:inline">Home</a>
            <a href="#" class="text-gray-300 hover:text-amber-400 block lg:inline">Create Character</a>
            <a href="#" class="text-gray-300 hover:text-amber-400 block lg:inline">Saved Characters</a>
            <a href="#" class="text-gray-300 hover:text-amber-400 block lg:inline">Campaigns</a>
            <a href="#" class="text-gray-300 hover:text-amber-400 block lg:inline">Tutorials</a>
            <a href="#" class="text-gray-300 hover:text-amber-400 block lg:inline">Account</a>
            <a href="/login" class="text-gray-300 hover:text-amber-400 block lg:inline">Login</a>
        </div>
    </div>
</nav>